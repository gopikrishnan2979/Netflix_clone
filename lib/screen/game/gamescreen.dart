import 'package:flutter/material.dart';

class GameScrn extends StatelessWidget {
  const GameScrn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
    );
  }
}
